stages:
- name: Build
  steps:
  - publishImageConfig:
      dockerfilePath: ./Dockerfile
      buildContext: .
      tag: ' bjornadalno/nginx-rp-letsencrypt:${CICD_GIT_COMMIT}'
      registry: index.docker.io
timeout: 60
notification: {}
